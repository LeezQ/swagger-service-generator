import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'controller.dart';

class <%= data.name%>Page extends GetView<<%= data.name%>Controller> {
  <%= data.name%>Page();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('<%= data.name%>Page')),
        body: SafeArea(
            child: Obx(() => Text('<%= data.name%>Controller x ${controller.name}'))));
  }
}
